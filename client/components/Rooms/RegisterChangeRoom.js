import React from "react";
import { useNavigation, useRoute } from '@react-navigation/native';

const RegisterChangeRoom = () => {
    const nav = useNavigation();
    const route = useRoute();  
};

export default RegisterChangeRoom;